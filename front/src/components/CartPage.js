import { useState } from "react";
import ProductCard from "./ProductCard";

const CartPage = () => {
    return (
        <div>
            <h1>Carrito</h1>
        </div>
    );
};

export default CartPage;